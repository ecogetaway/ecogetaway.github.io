# Disabled while GitHub Pages is publishing from the main branch again.
# The github-pages deployment API queue was timing out; keep this file for
# a future switch back to Actions-based publishing if needed.
name: Deploy GitHub Pages

on:
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  deploy:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "disabled"
